// Every Java program must reside inside a class. 
// The class name must exactly match the filename (e.g., HelloWorld.java).
public class HelloWorld {
    
    // The main method is the entry point where execution begins.
    public static void main(String[] args) {
        
        // Prints text to the console screen followed by a new line.
        System.out.println("Hello, World!");
    }
}


